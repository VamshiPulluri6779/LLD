package BatteryChargingLLD.Device;

import BatteryChargingLLD.BatteryStrategy.DeviceStrategy;

public class AudioDevice extends Device{

    public AudioDevice(DeviceStrategy deviceStrategy, int battery){
        super(deviceStrategy, battery);
    }
}
